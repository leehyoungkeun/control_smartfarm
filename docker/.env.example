# ==============================================================================
# 스마트팜 클라우드 서버 환경변수 설정
# 이 파일을 .env로 복사한 후 실제 값을 입력하세요
# cp .env.example .env
# ==============================================================================

# ── 서버 설정 ──────────────────────────────────
NODE_ENV=production
PORT=3000
FRONTEND_PORT=80

# ── PostgreSQL 데이터베이스 설정 ────────────────
DB_HOST=postgres
DB_PORT=5432
DB_NAME=smartfarm_cc
DB_USER=postgres
DB_PASSWORD=여기에_안전한_비밀번호_입력

# ── JWT 인증 설정 ──────────────────────────────
JWT_SECRET=여기에_랜덤_시크릿_키_입력
JWT_EXPIRES_IN=24h

# ── AWS IoT Core 설정 ─────────────────────────
# 인증서 파일 경로 (컨테이너 내부 경로)
AWS_IOT_CERT_PATH=/app/certs/certificate.pem.crt
AWS_IOT_KEY_PATH=/app/certs/private.pem.key
AWS_IOT_CA_PATH=/app/certs/AmazonRootCA1.pem
AWS_IOT_ENDPOINT=여기에_AWS_IoT_엔드포인트_입력.iot.ap-northeast-2.amazonaws.com
AWS_IOT_CLIENT_ID=smartfarm-cloud-server

# 호스트에서 인증서 디렉토리 경로 (docker-compose 볼륨 마운트용)
AWS_IOT_CERTS_DIR=./certs

# ── SMTP 이메일 알림 설정 (선택사항) ────────────
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=앱_비밀번호_입력
